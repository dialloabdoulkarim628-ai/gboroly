// Config ESLint racine — réexporte la config partagée @gboroly/config.
// Découverte par chaque package/app via la remontée d'ancêtres (ESLint flat config).
export { default } from '@gboroly/config/eslint';
